import {FETCH_DATA} from '../actions/getTree';

const treeData = (state = [],action)=>{
	switch(action.type){
		case 'FETCH_DATA':
			return state
		default:
			return state
	}
}

export default treeData;
