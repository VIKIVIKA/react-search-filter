import {GET_TREEDATA} from '../actions/getTree';

const treeData = (state = [],action)=>{
	switch(action.type){
		case 'GET_TREEDATA':
			return {...state,treeResp: action.payload};
		default:
			return state
	}
}

export default treeData;

